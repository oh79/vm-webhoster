# 사용자 9의 웹 호스팅 프록시 설정
server {
    listen 80;
    server_name localhost;
    
    # 사용자별 웹 호스팅 라우팅: /9 -> VM 웹포트
    location /9 {
        # 경로 rewrite (/9/path -> /path)
        rewrite ^/9(/.*)$ $1 break;
        rewrite ^/9$ / break;
        
        # VM의 웹포트로 프록시
        proxy_pass http://127.0.0.1:8212;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 웹소켓 지원
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 타임아웃 설정
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # 루트 접근 시 사용자 목록 표시 (선택사항)
    location = / {
        return 200 '<h1>웹 호스팅 서비스</h1><p>사용자별 접근: /9</p><p>현재 활성 사용자: 9</p>';
        add_header Content-Type text/html;
    }
}

# SSH 포트 포워딩을 위한 stream 설정 (별도 파일에서 관리)
# SSH 접속: ssh -p 10026 user@localhost
